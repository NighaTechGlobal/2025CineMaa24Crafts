## Goal
- Increase the size of the contextual menu box shown on three-dot click in ProjectDetails.

## File
- `mobile-app/src/screens/projects/ProjectDetailsScreen.tsx`

## Changes
- Expand `menuDropdown` padding and set a `minWidth` to make the box noticeably larger.
- Increase `menuItem` vertical padding.
- Increase `menuText` font size.
- Slightly increase the icon sizes inside menu items.

## Constraints
- No commands executed.
- No README files created.

## Verification (static)
- Confirm style changes compile and menu container visually larger in code.